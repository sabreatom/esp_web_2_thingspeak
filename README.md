# esp_web_2_thingspeak

###Short description:
ESP8266 Arduino based sensor webserver and python script which reads sensor values from webserver and sends to ThingSpeak.

###Usage:
Needs Arduino IDE with ESP8266 and Python to be installed.
